{
    "outputDirectory": ".",
    "installCommand": "bun install",
    "buildCommand": "bun run build && bun run runtime",
    "rewrites": [
        {
            "source": "/api/webhook",
            "destination": "/App"
        }
    ]
}
